<Application x:Class="StayActive.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tb="clr-namespace:H.NotifyIcon;assembly=H.NotifyIcon.Wpf">
    <Application.Resources>
        <tb:TaskbarIcon x:Key="TrayIcon"
                         IconSource="/Resources/icon_inactive.ico"
                         ToolTipText="StayActive - Inactivo"
                         MenuActivation="RightClick"
                         TrayLeftMouseUp="TrayIcon_LeftClick">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem x:Name="ToggleMenuItem"
                              Header="Activar"
                              Click="ToggleMenuItem_Click"/>
                    <Separator/>
                    <MenuItem Header="Configuración" Click="OpenSettings_Click"/>
                    <Separator/>
                    <MenuItem Header="Salir" Click="Exit_Click"/>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Application.Resources>
</Application>